# Separate pilot grip and release source candidate

This folder is a review candidate. It does not replace the supplied hero, current pilot/exit animations, mesh weights, skeleton, ship or runtime. The paired sources are `PilotGripFit.glb` (4 seconds, 121 samples at30Hz) and `DisembarkGripFit.glb` (2.4 seconds, 73 samples). Use them together with the separate `SM_AcornShipGripFit` geometry when reviewing the proposed cockpit fit.

`ClosureNear.png`, `ClosureFar.png` and `ClosureOverview.png` show the corrected anatomical left palm and restrained finger closure. The right finger chain remains untouched; its source R_Middle1-to-Middle2 separation is abnormal, so this work does not mirror or rebuild it. The source hand roll uses its measured wrist-to-Middle1 axis, not an assumed Euler axis. The approved seated mount remains (-15,0,72)cm, yaw-90, scale1.5 in Unreal.

`ArmNaturalOverview.png` and `ArmNaturalRetract.png` are the current release views. Wrists lift to roughly105cm, retract behind the grips, then lower into the existing brace pose. Wrist rotation stays relative to its forearm, avoiding the folded cuff seen in the discarded world-orientation hold. `ArmHighOverview.png`, HighLiftDiagnostic.json and WorldHoldRelease.json preserve earlier diagnostic limitations; they do not describe the selected candidate.

Pilot changes only L_Wrist and its15 finger rotation tracks. Exit additionally changes L/R shoulder/elbow and R_Wrist rotations during the first13 samples. All translations and scales, all other rotation payloads, and all mesh/skin/material/node data are copied exactly from the respective immutable source. Every Exit track is exactly original from sample13 (0.433333s) onward. Thus the original lower-body pose, root/pelvis route, 0.82-1.6s actor travel, 1.6-2.4s planted stage and final Walk0.308333 pose are preserved by the source contract. Right finger local rotations are untouched.

SourcePairValidation.json independently verifies the original binary prefix, scene/mesh/skin/material data, original accessor/view prefix, 140 unchanged Pilot channel payloads, 135 unchanged Exit payloads, and matching candidate left-hand rotation bytes at t0. These facts establish source transport, not Unreal import or playback. Each GLB contains its original embedded mesh data because this proven import route uses a complete glTF source; **import animations only** so that embedded geometry never replaces TailV2 or the skeleton.

PairedRelease.json measures actual exported GLB interpolation at60Hz against the complete character mesh and both grip surfaces. BVH candidates are filtered by explicit triangle-edge intersection tests. Both grips have zero detected crossings from0.05s through0.433333s. Initial seated contact remains: at0s L78/R26 crossing pairs, at0.016667 L43/R0, and at0.033333 L52/R0. These counts are triangle pairs, not penetration depths. They are not a continuous-collision guarantee, a complete ship/self-contact check, or proof of a penetration-free Pilot grip. The geometry was fitted against the original hand orientation; the remaining seated contact must stay visible in review.

The source checks do not include the lead's phase-preserving live Pilot snapshot blend. That runtime blend must be exercised together with the new Exit and the fixed actor clock. No source-only handoff can certify arbitrary live Pilot phase continuity.

Rebuild source with the installed Blender: `blender --background --factory-startup --threads8 --python ContentSource/PilotGripPoseCandidate/AuthorPair.py -- --export`, then run `python ContentSource/PilotGripPoseCandidate/ValidatePair.py`. The generator depends on the preserved AcornShipGripFit.blend and TailCandidateV2.glb for previews. It emits compact rotation patches before reconstructing the separate GLBs. `--exit-only` is a bounded iterative option requiring the existing verified Pilot patch/source.

Native import requirements: existing geometry_0_Skeleton; animation-only Interchange; import skeletal/static meshes, materials, textures and physics disabled; no skeleton reference-pose update or T0 ref-pose replacement; sampling30Hz; save as new A_PilotGripFit and A_DisembarkGripFit; root-motion extraction and force-root-lock disabled. Verify both durations, the shared skeleton, source metadata, fresh persisted reload, and native combined grip/pose views with only the hero texture temporarily forced resident. Original A_Pilot and A_Disembark must remain unchanged.

Cuff torsion, a thin claw silhouette, and existing fur fragments remain. The source and native studio views remain provisional. They do not establish owner art acceptance, native gameplay behavior or performance.

The separate assets are now imported and freshly reloaded: A_PilotGripFit is4.0s and A_DisembarkGripFit is2.4s, both on the existing geometry_0_Skeleton with root-motion extraction and force-root-lock disabled. UnrealPairImport.json preserves the original new-only import; UnrealPairPersisted.json records the later independent editor reload. UnrealPairPreview.json binds the executed script revisions, imported package hashes, 148 unchanged Source/Config/Content files, four image hashes and36 native bone comparisons.

The successful native images are UnrealResidentBefore.png, UnrealResidentAfter.png, UnrealReleaseLift.png and UnrealReleaseRetract.png. Before uses the original ship grip/Pilot; After uses both the fitted grip and repaired Pilot, intentionally changing both geometry and pose. The release views use the new Exit at0.1s and8/30s. Each screenshot first proves the active animation/time and nine actual component-space sockets against independent compressed AnimPose evaluation. Maximum differences were0.002729cm and0.007672degrees. The left wrist differs170.2563degrees from the original seated pose. MatchedResidency.json binds the identical full2048x2048/2752KiB DXT1 hero texture rows before all four shots; only that texture was temporarily forced resident, with no saved streaming flag changes.

InitialNativeStalePose preserves the rejected earlier four-view run: its clip loading checks passed but all images showed the initial Pilot pose. OverrideAnimationData writes saved animation settings without replacing an already initialized SingleNode instance. The helper now explicitly calls SetAnimation and SetPosition before refresh, then asserts actual pose differences. The first strengthened-guard retry also failed before screenshots because CurrentAsset is not exposed as a Python property; the corrected public GetAnimationAsset accessor is used in the successful pass. These failures are retained rather than relabeled successful.

The native repaired seated pose is visibly palm-down. Release samples clearly lift and retract the hands; the motion is brisk and high, and retraction partly hides the far hand behind the torso. Initial skin/grip intersections, cuff torsion, claw silhouette and fur fragments remain. No still image or triangle count certifies exact grasp or natural animation quality. The lead's live-phase snapshot blend and gameplay exit must still be exercised together with this pair.

SwiftGripClearance.json compares the exact exported pair against both the fitted grips and the actual original grips in SwiftCandidate.blend. The Swift grips have zero detected crossings from1/60s through0.433333s; fitted grips clear from0.05s. At five seated phases, original Swift grips produced8-23 left and0-3 right triangle pairs, versus73-80 and0-55 with the fitted grips. Those counts do not measure depth or prove visual contact, but show no collision reason to transplant the fitted geometry. Swift source remains unchanged.

Routine pipeline receipts use Saved/Validation/PilotGripFitImport.json, PilotGripFitPersisted.json and PilotGripFitPreview.json; residency extraction is recorded in PilotGripFitResidency.json. Historical final review copies remain in this folder. PipelineRevision.json separates the executed compact scripts from the later readable UTF-8/LF revision; the lead will re-exercise those modules in combined content validation.

The grip importer uses Scripts/SourceDigests.py for reviewed OBJ/MTL identities and existing source metadata. Only complete uniform LF/CRLF UTF-8 representations are equivalent; geometry edits, whitespace changes and binary changes remain failures. Raw source and package hashes in execution receipts are never rewritten.
