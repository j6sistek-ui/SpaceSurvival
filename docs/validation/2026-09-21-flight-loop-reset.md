# September 21 flight-loop reset validation

Status: **PARTIAL — Build 21 passed all 91 project automation tests again with the final authored station revision and repaired colony materials. Windows Package 2 and its selected IoStore/dependency audit passed after the retained first cook failure, but its packaged Station5 run exposed a missing plugin material.** Station render cycle 2 passed its scripted integrity/gameplay checks but did not meet final art acceptance. Clean packaged runtime verification, final visual review, natural play and owner acceptance remain open.

The candidate is the isolated `codex/flight-loop-reset` worktree, based on `684db34` (PR #58). Draft [PR #59](https://github.com/j6sistek-ui/SpaceSurvival/pull/59) binds Build 21 and its 91-test result to native source checkpoint **`0768e2c8d76e974ff56b6414edbadbd177c4ccf0`**. The native source was uncommitted when those checks ran and then committed unchanged; the successful packaging checkpoint is **`8aa2fc1c74642ae74daec79c1b371b052343b81d`**. Earlier CI/build results remain separate checkpoints. Licensed derivatives and the reset package are local artifacts, not a GitHub download. Original vendor assets are preserved. No merge or itch update is recorded. [KNOWN_ISSUES](../KNOWN_ISSUES.md), RPT-20260921-01, owns current work and acceptance.

## Connection and diagnosis

The existing Nwiro endpoint `http://localhost:5353/mcp` negotiated protocol `2025-03-26` and identified `nwiro 1.0.0`; `tools/list` returned 482 tools. The first tool call listed the skill library; the next fetched BlueprintBasicsSkill, DefaultOutdoorLightingSkill, MaterialBasicsSkill and UnrealSkillBestPracticesSkill. Inspection identified SpaceSurvival in UE 5.8.2 with PIE stopped. This verifies local HTTP MCP access; the chat's native inventory did not expose Nwiro. Receipts are in the original checkout's ignored `Artifacts/McpReview`.

Repository onboarding, CLAUDE, GAME_SCOPE, IMPLEMENT and relevant project-adapted gameplay/debug/C++/Blueprint skills were read. Installed engine and ShipCore source supplied version-specific API checks.

Confirmed defects included bypassing the supplied Blueprint hierarchy, holding the main animation at its last frame, a muzzle inside the larger hull, landing guidance aimed at the hub origin, minimum-speed braking in the station zone, launch respawn and boost force after domain rejection. Testing also exposed undersized flight collision, oversized parked-foot collision and stale thruster timing. The owner's original runtime remains unconfirmed; these findings do not establish one cause for every reported symptom.

## Build and automation history

| Checkpoint | Result and retained evidence |
| --- | --- |
| Initial compilation/controller | Comparator and fixture API errors repaired; builds 4/5 passed in 5.20s/22.51s. First ControllerToPhysics run reached real mouse yaw/pitch 36.69°/18.26° and gamepad 42.64°/16.55°, but failed on raw vendor demo references, AirBrake metadata and an invalid fixture LocalPlayer outer. `Artifacts/ControllerTests/index.json`. |
| Presentation authoring | First dry run stopped on an animation-data API assumption before saving. Revision 2 later compiled and retained all 30 supplied scene components, with original Blueprint/AirBrake bytes preserved. |
| Builds 7–8 | Build 7 passed in 11.59s. `ControlRigTests2` had four assertion passes carrying initialization warnings and one parked-collision failure. Pre-registration defaults and measured gear collision replaced those assumptions. Build 8 passed in 17.15s; `GearGeometryTests` passed 1/1 cleanly. |
| Builds 9–11 | Builds 9/10 failed on a test-only const API mismatch and missing PhysicsCore dependency; both repaired. Build 11 passed in 22.62s. |
| Build 12 | Passed in 26.13s. `ResetIntegrationTests1`: **8/8 clean**, covering hull hazards/projectiles, physical hull collision, docking admission, asteroid/station collision, staff and wardrobe. Real wing impact reached 64.14°/s before gyro recovery; mass, centre-of-mass and inertia assertions passed. |
| Full suite 1 | `ResetFullTests1/index.json`: **78 clean passes, 8 failures, 0 omitted** of 86. Failures: BoostBrakeMovement, DirectionalDodgeCollision, ProjectileRelativeMotion, StationDeparturePause, AcceleratedTenWaveJourney, ShipPresentationSelection, StationServiceLabelView, PhoenixParkedCollision. |
| Build 13 | Passed in 19.19s. `ResetRegressionTests2`: **6 clean passes, 1 failure**. Brake, classic projectile, departure-pause, journey, hull-selection and label checks passed after repairs. Dodge still failed its conservative rotated-box oracle. |
| Build 14 | Passed in 26.34s. Actual convex-support dodge assertions passed in the subsequent full run. Private parked physics and asteroid material authoring completed. |
| Full suite 2 | **CRASHED; no final report.** `BuildLogs/ResetFullTests2.log` records ContactImpactResponse failure, then successful pause and accelerated-journey assertions. After AlienGalleryLifecycle teardown, garbage collection asserted `UObjectArray: Index >= 0`. |
| Triage 3 | `ResetTriageTests3/index.json`: **2 clean passes, 1 failure**. Isolated AlienGalleryLifecycle and PhoenixParkedCollision passed. All three exact-foot world capsule sweeps, clear starts, neighboring walking space and retained hull/ramp checks passed. ContactImpactResponse failed. |
| Build 15 / Triage 4 | Build passed in **11.50s**. `ResetTriageTests4`: **5 clean passes, 1 failure**. ClassicProjectileRelativeMotion, HullHazardContacts, HullProjectileContacts, AlienGalleryLifecycle and strengthened PhoenixParkedCollision passed. ContactImpactResponse failed. StationDeparturePause was not selected. |
| Build 16 / Triage 5 | Build passed in **8.15s**. `ResetTriageTests5`: **2 clean passes, 1 failure**. StationDeparturePause passed including immediate forced garbage collection, followed by AlienGalleryLifecycle. ContactImpactResponse retained a 30Hz-versus-120Hz forward-recovery discrepancy. |
| Build 17 / full flight suite | Build passed in **10.52s**. `ResetFlightTests6/index.json`: **24/24 clean, zero warnings/failures/omitted**. Includes controller-to-physics and after-takeoff control, boost/brake, frame-rate trajectories, hull hazards/projectiles, directional dodge, manual weapons, contact/swept-impact response and departure pause with forced GC. Contact response at 30/60/120/144Hz was exactly (0, −600, 0) cm/s, with quarter-second residual Y −209.963cm/s at every rate; original test tolerances were retained. |
| Build 18 / full suite 3 | Build passed in **1.93s**. `ResetFullTests3/index.json`, created **07:18:43 UTC**: **84 clean passes, 2 failures, 0 omitted**. AcceleratedTenWaveJourney failed physical pad support and used the interior fallback at both stations; StationResetCollision failed native collider identity/bridge-barrier assertions. |
| Focused station run 7 | `ResetStationTests7/index.json`, created **07:20:30 UTC**: Journey passed on the same Build 18 binary, StationResetCollision failed again. This isolated pass did not close the order-dependent arrival defect. |
| Build 19 / full suite 4 | Build passed in **27.17s**. `ResetFullTests4/index.json`, created **07:30:43 UTC**: **87 clean passes, 1 failure, 0 omitted**. Stable collider identity tags and the bridge return guards passed; new PhoenixMooringPose and ClassicLaunchPresentation tests also passed. Journey again failed at both stations, now recording correct transforms but **physics=0** and no deck hit. |
| Build 20 / full suite 5 | Build passed in **22.45s**. `ResetFullTests5/index.json`, created **07:41:00 UTC**: **88/88 clean, zero warnings/failures/omitted**, test duration **30.994596s**. The complete preceding flight/presentation sequence was retained. `BuildLogs/Reset-FullTests5.log` records Cylinder and Cube **compiling=1 → compiling=0, physics=1**, followed by actual pad floor hits and **physics=1** at Waves 5 and 10. |
| Build 21 / full suite 6 | Build passed in **22.70s**. `ResetFullTests6/index.json`, created **07:56:30 UTC**: **91/91 clean, zero warnings/failures/omitted**, test duration **34.282787s**. New ControllerPitchParity, LiveRewardInput and StationArrivalPause all passed alongside the complete preceding suite. `BuildLogs/Reset-FullTests6.log` retains the execution details. |
| Final station assets / full suite 7 | The same Build 21 binary with the **08:15:55 UTC** station Blueprint and saved colony material repair passed **91/91 clean, zero warnings/failures/omitted**. `ResetFullTests7/index.json`, created **08:18:03 UTC**, records **34.394348s**; execution is retained in `BuildLogs/Reset-FullTests7-Unreal.log`. This verifies the final assets in the exercised native fixtures, not their final rendered appearance. |

The full-suite crash was traced using saved module offsets and installed Engine DLL exports to `FSubsystemCollectionBase::Deinitialize`, its destructor and `ULocalPlayer::~ULocalPlayer`, during garbage collection. Registering the pause fixture's local player initialized subsystems, but teardown omitted removal before destroying the world. Engine source requires deinitialization before UObject destruction. The fixture now removes its player first and explicitly collects garbage in StationDeparturePause. Triage 5 verified this repair and the following gallery test; full suite 5 subsequently passed the complete sequence. Crash artifacts remain under `Artifacts/ResetFullUser2/Saved/Crashes/UECC-Windows-B995F3FB4218AEF1B507069891B63C70_0000`.

The contact/flight repair aligns thruster force application before physics, samples hazard motion after physics, and applies the contact velocity delta immediately. The native adapter now supplies bounded XYZ velocity-error commands through ShipCore instead of combining cruise trim with its zero-speed dampeners. The old combination alternated full braking and released braking around zero trim, depending on frame rate. Exponential response removes that discontinuity while retaining the actual rigid body, calibrated mass, force limits and gyro control. Build 17's complete flight suite validates these assertions; natural feel remains a separate gate.

Earlier portable/source checkpoints passed **766 MSVC strict C++17 domain assertions**, **35 structural checks**, **13 source-digest tests**, Python compilation and content-source validation (26 meshes, 17 WAVs, original GLB unchanged). Documentation checks passed then. Docker was stopped locally; local GCC/ASan/UBSan were not run. PR #59 at `111c011` passed container core and documentation jobs; its source job stopped on two inherited formatting differences. Those were corrected, and the lead verified all source/core/documentation CI checks successful at `eb93770`. These commit-scoped CI results do not validate later uncommitted changes or substitute for Unreal tests.

### Input and menu corrections after Build 20

Installed `FSceneViewport::OnMouseMove` already converts physical mouse-up to positive MouseY, matching positive right-stick Y. The old extra mouse negation reversed flight pitch; the walker's subtraction then reversed the shared look direction again. ControllerPitchParity now verifies mouse/stick up in both actual flight and walking contexts, normal and inverted settings, plus the configured stick deadzone. It injects engine key/axis events and processes the real controller; it does not operate physical devices.

Live flight rewards now request GameOnly permanent mouse capture with a hidden cursor and disclose Up/Down or D-pad navigation plus Enter/A confirmation. LiveRewardInput supplies a real but unattached `UGameViewportClient`/Slate viewport, exercising the engine input-mode branch: capture policy, continued ship steering, keyboard/controller selection, exactly-once reward acceptance, and rejection of hidden/stale pointer actions without firing all pass. Depot and ordinary menus retain their cursor policies. The fixture creates no native window and does not prove OS capture acquisition, focus restoration or physical mouse behavior.

Menus now also pause incoming Docking while the controller still possesses the ship. StationArrivalPause runs actual Phoenix landing movement/animation and the production three-second domain timer: all hold through 60 paused frames, resume on closing Main, and complete the real EnterStation handoff to a supported exterior walker beside the same parked ship. Registered local-player teardown and forced garbage collection pass. Production account initialization is bypassed and persistence is blocked in these transient fixtures.

## Phoenix assets and verified collision

Private `BP_PhoenixPresentation` preserves the supplied scene hierarchy while removing broken demo gameplay. Its revision-2 author disabled physics/collision/autostart defaults before registration and saved corrected private AirBrake data. Native tests exercise the actual rig and authored three-second landing/takeoff sequences. The native ship remains the flight authority.

| Private output | Measured scope and SHA256 |
| --- | --- |
| `DA_PhoenixFlightHull` | **11 shapes**: nine retained rigid hull shapes and two bounded nacelle envelopes. Three deployed-foot envelopes and open ramp excluded. Additional geometry welds into the calibrated root body without contributing mass. `f0651be8c446d9b41115290e7b4135eca7bf075a067229a390417f7f315c6464`. |
| `PA_PhoenixParked` | **10 original shapes** retain hull, mounts and deployed ramp; only oversized foot boxes **0, 10, 11** removed. **24 measured bone-attached part bounds** own animated gear contact. `4171a5c02e718da6909a75971f58065720dc0ebca332e25c9b5836640968e2a3`. |

Both live under `/Game/SpaceSurvival/Licensed/PhoenixPresentation`; receipts are `Artifacts/PhoenixPresentation/flight-hull-author.json` and `parked-physics-author.json`. Four flight inputs and both parked inputs were hash-preserved. Original PhysicsAsset SHA: `d983774bf4a08661cb19243842e8b96c5a63cc132fc2f188e2c5766c56198b79`; skeletal mesh SHA: `4a2b902bfa283124798c8a2f5aded96cb89d71da302c8d790d6719ac10be0134`. Runtime overrides the parked component's PhysicsAsset, leaving its source mesh unchanged.

The raw front-foot envelope spanned about **970cm** across a visible foot about **92cm** wide. Actual deployed vertices set Phoenix dock clearance to **2.5cm**, replacing 230cm. Triage 3/4 verify grounded feet, exact-foot walking contact, clear adjacent space and removal of parked collision before takeoff. The strengthened posed-triangle check rejects absent geometry and positively locates front-foot triangles at deck height before checking the neighboring clear column. This does not promise standing headroom everywhere beneath the fuselage.

StationPhoenixPaintCapability passed in full suite 1. Phoenix materials expose no supported per-section color controls; the service reports factory finish unavailable for customization and rejects disabled/stale choices before account mutation. Classic account colors remain intact. A Phoenix paint shader remains open.

Full suite 5 also verifies that releasing a depot's magnetic hold keeps the Phoenix gear/ramp stowed without rewinding its flight animation, while a real pad departure still advances the takeoff clip. ClassicLaunchPresentation exercises the reused classic pawn after a loadout change: the selected Agile hull, pilot visibility and flight light return, the pawn lifts, and run identity/upgrades remain intact. These actual component/pose assertions do not establish animation quality in motion.

## Asteroid station and rendered review

Revision-3 asteroid authoring preserved the Photo 5 source SHA `d816fe0e57174bcd50f9cc733489117adc48f2078a538e1bc7e29cf2bec399b9`. Its **5,464,576 source/Nanite triangles** differ from the old 23,194-triangle fallback. Private editor source remains available; cooked Nanite geometry is **500,640 triangles**, with a separate **150,192-triangle** double-sided ComplexAsSimple fallback preserving the hollow bowl. Geometry-output SHA: `f8df964cd725b624b1bc95cb6ace8e40c7efa5816e3e8bed662c6ab1849fdb77`.

The first transient 500k reduction stopped at an unavailable factory API before saving; 20k/50k/100k collision targets failed the fixed 1cm source-scale tolerance. The accepted fallback preserved all 175 sampled ray-hit states, maximum surface delta 0.773376cm and bounds delta 0.170021cm. At scale 145 the sampled displacement reaches **112.14cm**; sparse probes are not complete fidelity proof. Placement inspection checked 213 room/terrace samples and two cameras/target rays against built collision, with unchanged bytes. Native asteroid collision passed in the Build 12 focused run.

The first `BP_StationReset` compiled with **239 meshes, 30 box specifications and 10 services**, SHA `8de7710029369967b95ed2d7fb134bc95f2b403eb601414b393b512f0eb041ad`. Original station SHA `ea4484a491c2d47ff04a01f88e64aef688ac303bd1bcf56529623d9fcb5ba02c` was preserved. These counts describe that authored revision, not later recipe edits.

Station5 render **`351c8cbd27704d1ea843530265768460`** passed functional/integrity checks: scripted ordinary approach and `GameMode.Interact` docking request, approximately 2.99s docking, Squirrel standing on deck for 25s, zero off-deck rescues, no save-slot writes, actual Phoenix metadata and hidden fallback confirmed. It used seeded progress, upgraded equipment and enlarged durability; it is not natural progression or physical input.

**Lead and independent review rejected its visual quality:** overlit tan rock, light sculptures reading as consoles, and crate-like terrace structures. Functional success does not close that rejection. Console/UI pairs, bay details and colony architecture were subsequently revised as recorded below; another rendered review is required.

Private rock material `/Game/SpaceSurvival/Licensed/StationReset/Materials/MI_StationAsteroid` was subsequently authored: brightness 0.18, saturation 0.1, tiling 2.5, roughness 0.65–0.95, specular 0.25, metallic 0, tint (0.55, 0.7, 1). Output SHA `c3a134c090cf516d5c0248d5902b6e3e792b225877384b3a043fcf0709f394af`; source SHA `48299379a1ed0cd3b726ccf7c8b152b69e8e498295f326156cd7045ec0eea4c5` preserved. `Artifacts/StationAsteroid/material-author.json` proves authoring, not visual acceptance; the rejected capture predates the correction.

Earlier Wave1 capture `2faa0187356c4471b1b013cccf812b3d` showed the full Phoenix and brake deployment. Its receipt incorrectly named the hidden fallback; later station metadata corrects that field. The older images remain evidence only for their captured revision.

### Revised station authoring and arrival regression

Build 18 restricted NwiroIntegrationKit and UAssetBrowser to Editor targets; runtime gameplay was unchanged from Build 17. Colony import completed with source preservation: **307,944 Nanite triangles**, **46,918 fallback triangles**, two PBR materials/six 2K textures, dimensions **15.24 × 18 × 10.67m** and a base pivot. Mesh SHA `8b8403de24faf61ebcf68eac44a076e25d8bafe32aac1aa12db0126b44ae3141`. Original Figur Blender SHA `3913fd79109fa0fa086e50fa1b35f14f15b585c1b3d1ce07086bea277a3ced72` remained unchanged. The private derivative removes the hanging mast beneath the ring; the source is retained. `Artifacts/StationReset/ColonyHabitat/author.json` and `import.json` bind the extraction, portable material bake and actual Unreal import. Two decorative assemblies sit on the existing bounded terraces; this does not add playable rooms.

The station revision prepared for render cycle 2 saved **282 meshes, 45 box specifications and 10 services**, SHA `c3962ab9e152ccda42792c4816a66c524a847c10bb55dcb284c640cf2d6ff1b9`; original station bytes were preserved. The **07:29:17 UTC** reauthor retained those counts and saved SHA `b7561b2dfcd33f94a9a1a6ada24faf788881d5a352c7ee9fd325c31b40806d2e`, with the preceding private output identified in `Artifacts/StationReset/ownership.json` and the same original SHA preserved. This includes actual curved console/UI pairs, distinct service bays, localized warm lights, gray rock override and grounded rounded buildings. The subsequent capture is assessed separately below.

The full-suite 3/4 failures above are retained. Stable collision tags now identify authored solids without depending on Unreal's generated UObject name suffixes; actual standing-capsule sweeps also verify the circular-pad/bridge return guards. Full suite 5 passes this coverage, including the physical station floors, walls, consoles, staff and home/station wardrobe interactions.

The arrival defect was **mesh compilation readiness**, not the parked ship obstructing the exit. Full suite 4 showed both correctly positioned deck components with no physics state, before the parked collision could explain the miss. Installed UE 5.8 source explicitly suppresses `UStaticMeshComponent::ShouldCreatePhysicsState()` while its mesh is compiling. `ASSLandingPad::AddMesh` now finishes pending compilation of each critical solid mesh before component registration under `WITH_EDITOR`; compiled cooked assets do not take that path. Full suite 5 directly observed both pending BasicShapes assets becoming ready, immediate physics creation, and successful supported exits at both stations. The fix preserves circular geometry and all original floor/capsule assertions, with no arbitrary sleep or weakened tolerance.

Source checks (36), recipe checks (8), digest tests (13), Python compilation/content-source checks, cook coverage (129 paths / 35 roots / no gaps) and native formatting passed at the preceding source checkpoint. Cook coverage is a reference audit, not a completed package or packaged runtime check. Build 20 retains the installed toolchain's nonpreferred compiler warning separately from the clean automation report.

### Render cycle 2 and material repair

Station5 capture **`45ea22a5885646f2adf5ad427fb58a57`** ran `UncookedEditorGame` at source `0768e2c` and exited 0. Its `result.json` verifies unchanged source/artifacts, preserved production saves and no fixture save-slot writes. The fixture observed the requested docking, **2.997793s** of docking, **25.002904s** standing on deck and **zero off-deck rescues**. It is a seeded, offscreen transition fixture with audio disabled and is explicitly unsuitable for performance findings.

Lead and independent visual review found the interior improved, but **final art acceptance was not met**: the pad/ship was difficult to read in shadow, the hall exterior remained flat, and colony buildings rendered with default material. `Rendered.log` lines 1669–1672 (repeated at 1781–1784) confirmed that both private `M_ColonyHabitat_0/1` materials lacked Nanite usage, causing Unreal's default-material substitution. A successful fixture receipt did not hide this rendering failure.

The guarded material-only author completed at **08:07 UTC**, after a dry run, using `ImportStationColonyHabitat.py -SSRepairStationColonyMaterials -SSApplyStationColonyHabitat`. `Artifacts/StationReset/ColonyHabitat/material-usage-author.json` records **false → true** Nanite usage on both private materials, matching prior ownership hashes, backups of the packages and prior import receipt, and refreshed ownership hashes. Only those two material packages changed; the colony mesh, all six textures and original Figur source stayed byte-identical, with no geometry/texture reimport. Resulting material SHA256 values are `fe3ba6f45f5ede55643d0548bb8d2760de7d33b02a8815e74c5ac078571a53c2` and `14a8854d4a5e832bef68e7ee9db1abdf35177dc5afe782256a25fbba0125c771`. This establishes persisted usage flags, not yet corrected rendered appearance.

The final bounded lighting/trim revision was saved at **08:15:55 UTC** as station Blueprint SHA **`995ddffa1c00e5bfbedf0aaf9922b8ac25c7e45bde377618b1a838f7edd341f6`**: **297 static meshes, 56 box specifications, 32 lights, 10 service anchors, two skeletal staff and two staff capsules**. The package hash was read back independently. `final-cycle-portable.json` records preservation of the prior 45 box specifications/services and clear central circulation, with 11 added solids and eight added lights. `final-trim-clearance.json` samples 135 points against the built asteroid collision fallback: none are inside rock, and the asteroid hash is unchanged. These are bounded geometry checks, not complete surface-clearance or aesthetic proof.

Full suite 7 subsequently passed all 91 tests using this final Blueprint and the repaired colony materials on the unchanged Build 21 native binary. Package 2 subsequently passed the separate checks below; the packaged Station5 assessment remains pending. The two earlier render cycles retain their own results and quality rejections.

## Windows package attempt 1

The first reset package attempt at source checkpoint **`7572c13736c06b22165c39c02687df869f0dc0cb`** failed during cook and produced no successful reset archive. `Artifacts/BuildLogs/WindowsPackage-d929e4d0ef0e49358caab55824917c02.log` retains the failed attempt. The cooker reports that `/Game/SpaceSurvival/Licensed/StationAssets/AlienCrew/RTG_MannequinToNyxar` references `/Game/SpaceSurvival/Licensed/MocapSource/IK_UE4Mannequin`, which is marked NeverCook. The authoring retargeter was included through the broad SpaceSurvival cook directory. The Unreal cook subprocess exited 1 after 233.88s; AutomationTool exited **25 (`Error_UnknownCookFailure`)**.

This is a packaging inclusion failure despite the clean editor automation and reference-coverage check. Exact, nonrecursive AssetManager NeverCook rules now exclude the authoring retargeters/rigs, unused Niagara backup material and raw AirBrake clip; runtime dependencies and the repaired private clip remain included. Source/core/documentation CI passed at `7572c13`, independently of this failed cook. The successful retry below does not erase it.

## Windows Package 2 and archive audit

Package 2 at **`8aa2fc1c74642ae74daec79c1b371b052343b81d`** completed with AutomationTool exit **0**, **141s** overall execution and **140.37s** BuildCookRun time. `Artifacts/BuildLogs/WindowsPackage-67d5e7488794495992fb75f4f8c3b3c7.log` records **3,006 cooked packages** and the commandlet summary **0 errors, 1 warning** (ShipCore's module-startup message logged at warning level). The compiler-preference messages and cook-display dependencies remain separate: the unused vendor `MI_AsteroidBarren_1` references absent `/Game/Materials/M_AsteroidBarren_1`, and Robot Scout's skeleton references the absent engine Humanoid preview package. These were not silently repaired or treated as proof of active runtime failures; selected runtime dependency closure passed the actual archive audit.

`Artifacts/FlightResetPackageAudit/Expected-86fc33c7a7604b6aa7768f8f05780e9a.json` froze **213 source/config/private-asset inputs** before packaging. The completed audit at **08:37:28 UTC**, `Audit-910f75e09f5c4252be45ff06e9503d8f/Audit.json`, records **6,558 index rows**, **2,998 exported/described packages**, **97 selected exports** and their **303-package dependency closure**, with **zero missing selected packages/descriptions/dependencies, unresolved imports or forbidden packages**. Both UnrealPak inspection commands exited 0 and the archive payload remained unchanged. The audit covers four private Phoenix assets, twelve private station assets, authored dependencies, Nyxar runtime assets, the runtime map and dynamically loaded dependencies; it is package-content evidence, not a rendered gameplay result.

The local archive is `Artifacts/Windows`, with **53 payload files / 5,588,675,264 bytes**. Its inner `SpaceSurvival/Binaries/Win64/SpaceSurvival.exe` is **335,003,648 bytes**, SHA256 **`99357796a59b081b1690ad835b739bff0a27a3443e73e715a974206f90acc263`**, independently read back after the audit. Bundled prerequisite signature/bytes passed, without installer execution or clean-PC testing. `Artifacts/FreshGit-reset-package2.json` verifies **747 exported files** at the same commit with unchanged bytes; source/core/documentation CI also passed at that checkpoint. The packaged-review launcher dry run passed according to the lead's execution record, without opening a native game window.

### Packaged Station5: fixture pass with retained runtime error

Capture **`76061c816e81494899f2c1cb5eda502f`**, completed **08:41:18 UTC**, ran `WindowsDevelopmentPackage` at source `8aa2fc1`. Its receipt reports exit 0, scripted success, all source/artifact/save guards passing, **2.996682s docking**, **25.001052s on deck**, **zero off-deck rescues** and 11 images. These are the packaged fixture results, not final visual or natural-play acceptance.

`Rendered.log` nevertheless records actual startup errors at lines **828 and 843**: `UWPPortalLightTransmissionSubsystem::Initialize` cannot load `/WormholePortal/WormholePortal/Materials/LightFunctions/M_WPPortalSphericalGate`. The selected archive audit did not cover this dynamically loaded plugin asset, so its pass cannot establish a complete runtime dependency set. This remains a real packaging defect despite the fixture's success. A bounded inclusion repair, new package/audit and clean runtime verification are pending. Final image review, packaged Wave10, physical input, synchronized listening, natural play and representative performance remain open.

## Evidence limits and remaining gates

- Build 21's complete project suite is clean, including contact recovery across frame rates, actual compound hull contacts, both station arrivals, wardrobe, presentation transitions and the three added input/pause regressions. Full suite 7 repeats that result with the final station Blueprint/materials. The earlier 88-test result remains a separate checkpoint before the input corrections.
- Synthetic input traverses the real controller, PlayerInput and ShipCore/Chaos; LiveRewardInput additionally exercises engine capture policy using an unattached Slate viewport. Production GameMode BeginPlay/account initialization is bypassed. Physical keyboard/mouse/controller use, native focus/capture acquisition, menu return, comfort and flight feel remain unverified.
- The accelerated ten-wave journey and seeded station capture disclose shortened phases, forced/seeded objectives, fixture positioning or durability changes. A complete natural Waves 1–10 run, both station visits, ordinary arrival/departure, save/resume and death-to-next-run acceptance remain open.
- Package 2 and its selected archive audit pass; packaged Station5 passes scripted transitions but logs the missing portal-material error above. Clean packaged runtime verification, updated station appearance, synchronized listening, representative 60 FPS performance, clean-machine launch, release/update behavior and owner acceptance remain open. Offscreen screenshots and reduced triangle counts do not establish those gates.
